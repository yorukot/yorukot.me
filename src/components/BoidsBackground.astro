<div class="boids-background" aria-hidden="true" data-boids-root></div>

<script>
    import { mountBoids } from '../scripts/boids';

    const root = document.querySelector('[data-boids-root]');

    if (root instanceof HTMLElement) {
        const cleanup = mountBoids(root);

        window.addEventListener(
            'pagehide',
            () => {
                cleanup();
            },
            { once: true },
        );
    }
</script>

<style>
    .boids-background {
        position: absolute;
        inset: 0;
        overflow: hidden;
        pointer-events: none;
        background:
            radial-gradient(circle at 50% 60%, rgb(199 199 199 / 0.06), transparent 42%),
            linear-gradient(180deg, rgb(255 255 255 / 0.01), transparent 25%);
    }

    .boids-background :global(canvas) {
        display: block;
        width: 100%;
        height: 100%;
        opacity: 0.7;
        mix-blend-mode: screen;
    }
</style>
